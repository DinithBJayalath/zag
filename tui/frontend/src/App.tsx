import TerminalView from './TerminalView';
import "./app.css"

function App() {
    return (
        <div className='main-div' style={{ height: "100vh", width: "100vw" }}>
            <TerminalView />
        </div>
    );
}

export default App
